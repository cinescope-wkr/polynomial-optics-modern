# Upstream README

The original upstream-style README is preserved as `README.upstream.md` in the repository.

This MkDocs page intentionally links to the repo copy to avoid duplication:

- `README.upstream.md`
